# Cloudflare Pages configuration
name = "engine-analysis"
compatibility_date = "2024-01-01"

# Build settings are configured in Cloudflare dashboard
# or via GitHub integration

[site]
bucket = "./dist"
